import React from "react";
import Layout from "../layouts/layout";
import Container from "../layouts/container";

const ProjectDetail = () => {
    return (
        <Layout>
            <Container top="pt-[160px] md:pt-[180px]">tes</Container>
        </Layout>
    );
};

export default ProjectDetail;
